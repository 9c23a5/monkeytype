import "jquery";
import Page from "./page";
import * as Notifications from "../elements/notifications";
import { Auth } from "../firebase";
import { applyActionCode } from "firebase/auth";

export const page = new Page(
  "emailHandler",
  $(".page.pageEmailHandler"),
  "/email-handler",
  async () => {
    //
  },
  async () => {
    //
  },
  async () => {
    //
  },
  async () => {
    //
  }
);
